.git
.github
assets
mcp-server
scripts
docs
.claude
content
# NOTE: .devcontainer must NOT be ignored — the container-scan workflow builds
# .devcontainer/Dockerfile with the repo root as context and COPYs
# .devcontainer/init-firewall.sh from it.
